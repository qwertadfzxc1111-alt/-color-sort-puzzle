# البنية التقنية

المشروع يستعمل قالب React/Vite كغلاف توزيع، لكنه يحافظ على منطق اللعب في **Canvas وJavaScript صريح** كما طلب المنتج. لا توجد قاعدة بيانات أو واجهة خادم أو تبعيات ألعاب خارجية.

| المسار | المسؤولية |
|---|---|
| `client/src/pages/Home.tsx` | غلاف React صغير يركب اللعبة ويعرض فتح المرحلة أو إكمالها. |
| `client/src/game/script.js` | صف `ColorPourGame` المستقل: رسم Canvas، الإدخال، حالة المراحل، الحركات، التراجع، والحفظ. |
| `client/src/style.css` | قواعد الهوية البصرية، تخطيط الهاتف العمودي، النوافذ، وأزرار التحكم. |
| `client/public/levels.json` | بيانات المراحل القابلة للتحرير من دون تعديل المنطق. |
| `client/public/manifest.json` | إعدادات PWA والاسم والألوان والعمل في الشاشة الكاملة. |
| `client/public/sw.js` | تخزين واجهة اللعبة والبيانات للاستخدام دون اتصال بعد أول تحميل. |
| `client/public/config.xml` | أساس تغليف APK عبر Cordova/Capacitor. |

## نموذج الحالة

تكون حالة اللعبة كائناً يحمل رقم المستوى، سعة الأنبوب، مصفوفة الأنابيب، الأنبوب المحدد، مصفوفة تاريخ النسخ العميقة، وعدد الحركات. كل أنبوب مصفوفة لون مرتبة من الأسفل إلى الأعلى. لا يستعمل المنطق حالة React لحركة اللعب عالية التردد؛ React يراقب أحداثاً معنوية فقط مثل فتح شاشة المراحل أو اكتمال المرحلة.
